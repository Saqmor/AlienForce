#include "VerriPG.h"


int main(){

    /*Graph grafo(5);

    grafo.add_edge(0,1,10);
    grafo.add_edge(0,3,30);
    grafo.add_edge(0,4,100);
    grafo.add_edge(1,2,50);
    grafo.add_edge(3,2,20);
    grafo.add_edge(3,4,60);
    grafo.add_edge(2,4,10);

    grafo.print_min_caminh(1,4);*/
    Menu menu;
    menu.run_menu();
}